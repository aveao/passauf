/target
/*.bin
/*.jpeg
/*.jp2
# ICAO 9303 PDFs, kept locally for reference.
/standards
# Local planning notes.
/PRE-RELEASE-PLAN.md
# Corpora, renders, checkpoints and models. All reproducible from train-ocrb.sh, and
# large enough that committing them would dwarf the source.
/tesseract-ocrb-passauf/build
/tesseract-ocrb-passauf/target
# text2image scribbles this next to itself.
/tesseract-ocrb-passauf/fonts.conf
